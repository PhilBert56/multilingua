import { Injectable } from '@angular/core';

@Injectable()


export class lesson {

/*
Not in use for the moment, but should be used in the next version to be more rigourous with the Object model
*/

    id : number;
    objective :string;
    text :string
}